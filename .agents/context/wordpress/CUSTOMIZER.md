## Localize data
- don't recreate the settings token in customizer-preview.js, localize them from php and pass them in when customizer-preview is enqueued.


## Syntax
- instead of `var` use `let` or `const` in variable declarations, and also depending on if the variable will be altered further down the script